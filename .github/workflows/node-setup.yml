name: node-setup
on:
 push:
  branches:
   - master
 workflow_dispatch:   
jobs:
 setup:
  runs-on: ubuntu-latest
  steps:
   - name: node version check
     run: node -v
   - uses: actions/setup-node@v2.5.2
     with:
       node-version: 16.x
